{
  "name": "meta-the-world-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "contracts",
    "services/*"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "build:all": "npm run build --workspaces",
    "test:all": "npm test --workspaces"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
